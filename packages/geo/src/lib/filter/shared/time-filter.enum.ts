export enum TimeFilterType {
    DATE = 'date',
    TIME = 'time',
    DATETIME = 'datetime',
    YEAR = 'year',
    FORCED = 'forced',
}

export enum TimeFilterStyle {
    CALENDAR = 'calendar',
    SLIDER = 'slider',
}
